| 🧩 Метод / Класс         | 💬 Назначение                     | ⚙️ Что делает                                                 | 📍 Пример использования                                             |
| ------------------------ | --------------------------------- | ------------------------------------------------------------- | ------------------------------------------------------------------- |
| **`filter()`**           | Фильтрация записей                | Формирует SQL-условие `WHERE`                                 | `Book.objects.filter(price__lt=500)`                                |
| **`exclude()`**          | Исключение записей                | Противоположен `filter`                                       | `Book.objects.exclude(author="Толстой")`                            |
| **`get()`**              | Получение одной записи            | SQL-запрос с `LIMIT 1`                                        | `User.objects.get(id=1)`                                            |
| **`create()`**           | Создание записи                   | `INSERT INTO`                                                 | `Book.objects.create(title="Django")`                               |
| **`bulk_create()`**      | Массовое создание                 | Один SQL-вызов с множеством вставок                           | `Book.objects.bulk_create([...])`                                   |
| **`update()`**           | Массовое обновление               | `UPDATE` в SQL                                                | `Book.objects.update(published=True)`                               |
| **`delete()`**           | Массовое удаление                 | `DELETE FROM`                                                 | `Book.objects.filter(old=True).delete()`                            |
| **`annotate()`**         | Добавление вычисляемых полей      | Создаёт SQL-выражения с `GROUP BY`, `SUM()`, `COUNT()` и т.д. | `Book.objects.annotate(total=Count("id"))`                          |
| **`aggregate()`**        | Итоговые значения по всему набору | Возвращает одно значение                                      | `Book.objects.aggregate(Avg("price"))`                              |
| **`order_by()`**         | Сортировка на уровне SQL          | Добавляет `ORDER BY`                                          | `Book.objects.order_by("-date")`                                    |
| **`distinct()`**         | Убирает дубликаты                 | `SELECT DISTINCT`                                             | `Book.objects.distinct("author")`                                   |
| **`count()`**            | Подсчёт без выборки               | `SELECT COUNT(*)`                                             | `Book.objects.count()`                                              |
| **`exists()`**           | Проверка наличия записей          | `SELECT 1 FROM ... LIMIT 1`                                   | `Book.objects.filter(is_published=True).exists()`                   |
| **`F()`**                | Работа с полями внутри SQL        | Сравнение и арифметика между столбцами                        | `Book.objects.update(price=F("price") + 10)`                        |
| **`Q()`**                | Логические условия                | Позволяет комбинировать фильтры с `&`, `                      | `,` ~`                                                              |
| **`Case` / `When`**      | Условные выражения в SQL          | Аналог `CASE WHEN`                                            | `annotate(status=Case(When(price__gt=500, then="expensive"), ...))` |
| **`select_related()`**   | Жадная загрузка `ForeignKey`      | SQL-JOIN                                                      | `Book.objects.select_related("author")`                             |
| **`prefetch_related()`** | Предзагрузка `ManyToMany`         | Делает несколько SQL-запросов и объединяет                    | `Book.objects.prefetch_related("genres")`                           |
| **`only()` / `defer()`** | Отложенная загрузка полей         | Частичный SELECT                                              | `Book.objects.only("title")`                                        |