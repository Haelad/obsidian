
| 🧩 Метод / Функция               | 💬 Назначение                                                           | ⚙️ Где выполняется              | 📍 Пример использования                               |
| -------------------------------- | ----------------------------------------------------------------------- | ------------------------------- | ----------------------------------------------------- |
| **`all()`**                      | Возвращает QuerySet всех записей                                        | SQL-запрос без условий          | `Book.objects.all()`                                  |
| **`values()` / `values_list()`** | Преобразует результат в словари или кортежи                             | После SQL-запроса               | `Book.objects.values("title", "price")`               |
| **`iterator()`**                 | Последовательная загрузка без кеша                                      | Python                          | `for book in Book.objects.iterator(): ...`            |
| **`reverse()`**                  | Разворачивает порядок результатов                                       | Python                          | `Book.objects.all().reverse()`                        |
| **`first()` / `last()`**         | Возвращает первый или последний объект                                  | Частично SQL + Python           | `Book.objects.order_by("id").first()`                 |
| **`in_bulk()`**                  | Возвращает словарь объектов по ключам                                   | SQL + Python                    | `Book.objects.in_bulk([1, 2, 3])`                     |
| **`get_or_create()`**            | Получить или создать запись                                             | SQL + Python логика             | `Book.objects.get_or_create(title="Python")`          |
| **`update_or_create()`**         | Обновить или создать                                                    | SQL + Python логика             | `Book.objects.update_or_create(id=1, defaults={...})` |
| **`list()`**                     | Преобразует QuerySet в список                                           | Python                          | `list(Book.objects.filter(...))`                      |
| **`len()`**                      | Возвращает длину QuerySet                                               | Python (через `count()` внутри) | `len(Book.objects.all())`                             |
| **`for ... in queryset`**        | Итерирование по результатам                                             | Python                          | `for obj in Book.objects.all(): ...`                  |
| **`filter()`                     | Если используется к уже загруженным объектам (через list comprehension) | Python                          | `[b for b in books if b.price < 200]`                 |
📗 **Вывод:**  
Эти методы и операции не создают новые SQL-запросы, а **работают с уже полученными объектами Django** в памяти.  
Они нужны для:

- постобработки данных,
    
- экономии запросов,
    
- повышения читаемости кода,
    
- удобной логики без SQL-уровня.