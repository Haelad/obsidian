

```python
class addPostForm(forms.Form):
	title = forms.CharField(max_lenght=255)
	slug = forms.SlugField()
	content = forms.CharField(widget=forms.Textarea())
	is_pub = forms.BooleanField()
	cat = forms.ModelChoiceFeild(queryset=Category.objects.all())
```
все поля прописанные в классе обязательны для заполнения
html
```Html
{% extends 'base.html' %}
{% block content %}
<form action='' method='post'>
	{% csrf_token %}
	{{form.as_p}}
	<p><button type='submit'> Отправить</button></p>
</form>
```
![[Pasted image 20240709232439.png]]

через widget можно задать стили для полей формы

```python
title = forms.CharField(max_lenght=255, label='Заголовок',widget=forms.TextInput(attrs={'class': 'form-input'}))
```